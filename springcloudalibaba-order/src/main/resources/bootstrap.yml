spring:
  application:
    name: springcloudalibaba-order
  cloud:
    nacos:
      username: admin
      password: admin
      config:
        server-addr: localhost:8848
        file-extension: yaml