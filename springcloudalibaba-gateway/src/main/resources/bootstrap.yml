spring:
  application:
    name: springcloudalibaba-gateway
  cloud:
    nacos:
      username: admin
      password: admin
      config:
        server-addr: localhost:8848
        file-extension: yaml